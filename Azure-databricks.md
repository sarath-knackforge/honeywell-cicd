# Databrick clone repo from azure repot
# 1.    Go to databricks workspace -> repos (add or create git folder)

# 2.    if git repo clone has make any issue while clone follow below steps 
        1. Go databrciks user setting -> linked account => remove git credentials
        2. Then Azure deveops click Clone main branch copy URL same time generate Access token
        3. paste gnereate access token in linked account and give (email we use & add pat token)
        4. Then back to repos folder and clone it .